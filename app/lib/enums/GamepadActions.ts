export enum KeyboardAction {
  LEFT = "KeyA",
  RIGHT = "KeyD",
}

export enum ArrowAction {
  LEFT = "ArrowLeft",
  RIGHT = "ArrowRight",
}

export enum GamepadListener {
  TOUCH = "ontouchstart",
  KEYBOARD = "onkeydown"
}